extends Node

@onready var grid = $Grid

func _ready():
	pass
	#var screen_size = get_viewport().get_visible_rect().size
	# var grid_size = grid.get_used_rect().size
	# grid.position = (screen_size - grid_size) / 2
